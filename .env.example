# This is a fully static, front-end-only app — no database or secrets required.
#
# Optional: set your canonical site URL so sitemap.xml, robots.txt and Open Graph
# tags use absolute URLs. Defaults to https://australiaslang.futurebutnow.xyz.
# NEXT_PUBLIC_SITE_URL="https://your-domain.example"
